import * as FilterPayloadActions from './filter-payload.actions';

describe('FilterPayload', () => {
	it('should create an instance', () => {
		expect(new FilterPayloadActions.LoadFilterPayloads()).toBeTruthy();
	});
});
